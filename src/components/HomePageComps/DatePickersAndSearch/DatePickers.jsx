import { Input, DatePickerContainer } from "./styled";

const DatePickers = () => {
  return (
    <DatePickerContainer>
      <Input type="date" />
      <Input type="date" />
    </DatePickerContainer>
  );
};

export default DatePickers;
