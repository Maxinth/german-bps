import { Input, DatePickerContainer } from "styled";

const DatePickers = () => {
  return (
    <DatePickerContainer>
      <Input type="date" />
    </DatePickerContainer>
  );
};

export default DatePickers;
